# Forum-Auto API

**Base URL (v2 REST):** `https://api.forum-auto.ru/v2`

**Auth:** `login` + `pass` (query-параметры во всех запросах)

---

## clientInfo — проверка авторизации

```
GET /v2/clientInfo?login=ЛОГИН&pass=ПАРОЛЬ
```

Возвращает информацию о клиенте.

---

## listGoods — поиск запчастей

```
GET /v2/listGoods?login=ЛОГИН&pass=ПАРОЛЬ&art=АРТИКУЛ&cross=0
```

**Параметры:**
- `art` — артикул (служебные символы удаляются, регистр не важен)
- `cross` — 0/1, искать кроссы (аналоги)
- `br` — бренд (опционально, сужает поиск)
- `gid` — ИД товара в системе (опционально, поиск только по gid)

**Ответ:** массив объектов:
| Поле | Тип | Описание |
|------|-----|----------|
| gid | string | ИД товара в системе |
| brand | string | Бренд |
| art | string | Артикул |
| name | string | Наименование |
| d_deliv | int | Дней доставки |
| h_deliv | int | Часов доставки |
| kr | int | Кратность |
| num | int | Доступное кол-во |
| price | float | Цена |
| whse | string | Склад |
| is_returnable | int | Возврат (0/1) |

---

## addGoodsToOrder — корзина / заказ

```
GET /v2/addGoodsToOrder?login=ЛОГИН&pass=ПАРОЛЬ&tid=GID&num=КОЛ-ВО
```

**Параметры:**
- `tid` — gid из listGoods
- `num` — кол-во (должно быть кратно kr)
- `eid` — ИД строки заказа в вашей системе (опционально)
- `eoid` — ИД заказа в вашей системе (опционально)

**Ответ:** информация по заказу (аналогично listOrders).

---

## listOrders — информация о заказе

```
GET /v2/listOrders?login=ЛОГИН&pass=ПАРОЛЬ
```

---

## cancelOrder — аннулирование заказа

```
GET /v2/cancelOrder?login=ЛОГИН&pass=ПАРОЛЬ&gid=GID
```

## Коды ошибок

| Код | Описание |
|-----|----------|
| 0 | Веб-сервис недоступен |
| 1 | Неверный тип переменной |
| 2 | Метод не найден |
| 5 | Превышен лимит запросов в минуту |
| 6 | Превышен суточный лимит запросов |
| 10 | Неверный логин/пароль |
| 11 | Доступ запрещён |
| 12 | Клиент заблокирован |
| 13 | Превышен лимит суммы заказа |
| 20 | Товар не найден |
| 21 | Заказ не найден |
| 22 | Заказ нельзя аннулировать (статус) |
| 23 | Ошибка аннулирования |
| 24 | Заказ уже аннулирован |
| 25 | Ошибка создания заказа |
| 26 | Активные заказы не найдены |
| 27 | Товары не найдены |
| 28 | Неверное кол-во (меньше кратности) |
| 29 | Неверный eid/tid |
| 30 | Частичное бронирование |
| 31 | Предложение устарело |

**Важно:** количество запросов в минуту ограничено (ошибка 5 при превышении).
Полный функционал listGoods доступен на следующие сутки после получения логина.
